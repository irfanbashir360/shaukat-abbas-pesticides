// frontend/tailwind.config.js
// Note: Tailwind CSS v4 uses @tailwindcss/vite plugin for configuration.
// Content paths and theme extensions are handled via CSS @source and @theme directives.
// This file is kept for reference; v4 does not use tailwind.config.js.
export default {
  content: ["./index.html", "./src/**/*.{js,jsx}"],
  theme: { extend: {} },
  plugins: [],
}
